---
import Layout from './Layout.astro';
import { NavbarBlog } from '../components/NavbarBlog';
import { Footer } from '../components/SiteComponents';
import '../index.css';

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  ogType?: string;
  noindex?: boolean;
  publishedTime?: string;
  author?: string;
  faqSchema?: string;
}

const { title, description, canonical, ogImage, ogType, noindex, publishedTime, author, faqSchema } = Astro.props;
---

<Layout {title} {description} {canonical} {ogImage} {ogType} {noindex} {publishedTime} {author} {faqSchema}>
  <NavbarBlog client:load darkHero={true} />
  <slot />
  <Footer client:load />
</Layout>
